# Trading Analyzer Frontend Environment Configuration

# API Base URL - points to the Python FastAPI backend
# Note: Do not include /api suffix - it will be added by the app
NEXT_PUBLIC_API_URL=http://localhost:8000

# Disable mock data fallback - use real API only
NEXT_PUBLIC_USE_MOCK_FALLBACK=false
